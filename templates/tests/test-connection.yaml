apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubeflower-helm.fullname" . }}-test-connection"
  labels:
    {{- include "kubeflower-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubeflower-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
