apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emho-helm.fullname" . }}-test-connection"
  labels:
    {{- include "emho-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "emho-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
